all:
  # hosts:
  #   mail.example.com:
  children:
    gateway:
      hosts:
        localhost:
      vars:
        ansible_connection: local

    webservers:
      hosts:
        188.121.110.247:
    dbservers:
      hosts:
        192.168.1.204:

    elk_server:
      hosts:
        185.235.43.181:

    monitor:
      hosts:
        185.235.43.181:
